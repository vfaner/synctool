# SyncTool v1.1.2

异构数据库实时同步工具。开箱即用的可执行 jar，放到内网机器上 `java -jar` 就能跑，不需要源码、不需要 Maven。

## 本次更新：驱动随包内置 + 版本信息页

### 1. 十种数据库驱动内置，不再手动放 jar

以前除 MySQL 外，Oracle、SQL Server、DB2 等都要自己找驱动 jar 放到服务器上。这版把能在 Maven Central 取得、且许可证允许再分发的驱动全部随包内置：

- MySQL、MariaDB
- Oracle、SQL Server、DB2
- PostgreSQL、openGauss
- 达梦（DM）、人大金仓（KingBase）
- H2

选择这些类型后直接填连接信息即可，驱动 jar 卡片自动隐藏。各内置驱动的具体版本与许可证见发行包里的 [THIRD_PARTY_LICENSES.md](THIRD_PARTY_LICENSES.md)。

**GBase 与神通（Oscar）仍然需要外部 jar**：官方没有在 Maven Central 发布构件，本项目也未取得再分发授权。选中这两个类型（以及「自定义」）时，驱动 jar 卡片会自动出现，填厂商提供的 jar 路径即可；换内置类型时卡片隐藏并禁用，不会把旧 jar 路径一起提交上去。

### 2. 默认端口随数据库类型联动

修复了新建连接表单的一个问题：服务端预填的 3306 没有标记为「自动填充值」，第一次切换数据库类型时端口不会跟着变，必须手动改。现在选择类型后端口立即切换为该类型的默认值（编辑已有连接时保留保存的端口，不会覆盖）。

同一轮还修了几个相关边角：

- 隐藏的 jar 输入框以前仍会随表单提交，可能用一条旧的外部 jar 路径顶替内置驱动；现在隐藏即禁用，不会提交
- 「自动发现驱动类」以前会把结果写进被隐藏的自定义驱动字段，反而抢在预设驱动前面生效；现在只在自定义区块可见时填充
- GBase／神通这类必须带 jar 的类型，没填 jar 就保存或测试连接时，会直接给出明确的中文提示，而不是等到加载驱动时报一串英文异常

### 3. 新增「版本信息」模块

原来的「设置」菜单改名为**「系统信息」**（页面内容仍是只读的运行状态，没有可写操作，叫这个名字更贴切）。页面里新增「版本信息」卡片：

- **当前版本**：构建时写进 jar 的版本号；头部 logo 副标题行和页脚也会显示
- **GitHub 最新版本**：显示最新 release 的版本号和发布时间；与当前版本一致时显示绿色「已是最新」，有新版本时显示棕黄色「立即更新」，点击在新标签页打开 GitHub 发布页
- **当前版本更新内容**：当前版本的发布说明随 jar 打包，**纯内网也能看**，不依赖外网
- 检查中、检查失败、已关闭检查三种情况各有灰色状态提示，不会留空白

更新检查在后台线程进行，**打开页面永远不会等待网络**：启动后静默检查一次，结果缓存一段时间（成功 10 分钟、失败 1 分钟），过期后先显示旧结果再后台刷新。内网机器访问不了 GitHub 时，本地版本号和更新说明照常显示，只有「最新版本」一行显示「—」。纯内网环境也可以直接关掉检查：

```bash
java -jar synctool.jar --app.update-check.enabled=false
```

程序不会自动下载或替换安装包——「立即更新」只负责打开发布页，下载新版本、替换文件、重启仍由你自己掌控。

## 快速开始

```bash
java -jar synctool.jar
```

访问 <http://localhost:8080>，用下面的账号登录。

## 从 v1.1.1 升级

换 jar、重启，没有数据库结构变化，配置不用动。

```bash
sudo systemctl stop synctool
cp synctool.jar /opt/synctool/synctool.jar
sudo systemctl start synctool
```

**加密口令保持和之前一致**，否则已存的数据库密码无法解密。停机期间源库产生的变更会在重启后由游标机制自动补齐。

如果你以前为了连 Oracle、SQL Server、DB2、达梦、金仓等把厂商 jar 放进了 `./drivers` 目录：这些 jar 可以保留（不再需要，但不影响运行），也可以直接删掉。GBase、神通和自定义连接填写的 jar 路径不受影响。

## 从 v1.0.0 升级

v1.0.0 完全没有鉴权 —— 任何能访问到端口的人都可以调接口改数据库连接和 AI 配置。**建议尽快升级。** 鉴权是 v1.1.0 加的，升级前请先读 [v1.1.0 的发布说明](https://github.com/vfaner/synctool/releases/tag/v1.1.0)，那里有账号、角色和 CSRF 的完整说明。

## ⚠️ 首次启动必读

### 1. 立刻改掉初始密码

首次启动会在元数据库自动创建两个账号，**初始密码都是 `123456`**：

| 用户名 | 角色 | 权限 |
|---|---|---|
| `admin` | 管理员 | 全部操作 |
| `view` | 访客 | 只读 |

这个 jar 是公开可下载的，所以初始密码不是秘密。登录后点右上角用户名 → 修改密码。仍在用初始密码的账号，登录后每页顶部都会挂一条红色警告横幅，横幅只点名当前登录的这个账号。可以临时关掉，但浏览器一关又会回来，直到密码真的改了。

密码是 BCrypt 单向哈希，**忘记无法找回**。真忘了就删掉元数据库 `app_user` 表里对应那行，重启会重新种回初始密码。

### 2. 必须覆盖加密口令

jar 里内置了一个**公开的默认加密密钥**（`synctool-default-key-change-me`），它负责加密你存进去的每一个数据库密码和 AI API Key。用默认值等于没加密。

```bash
java -jar synctool.jar \
  --sync.crypto-password=你自己的强口令 \
  --sync.crypto-salt=你自己的16位十六进制盐
```

> **在第一次保存任何数据库连接之前就改好。** 已经存进去的密码是用旧口令加密的，事后换口令会导致这些密码无法解密，只能重新录入。

### 3. 数据目录

首次启动会在**当前工作目录**下创建 `./data`（H2 元数据库）、`./logs`、`./snapshots`。请固定在同一目录启动，或用绝对路径覆盖配置。

`./data/synctool.mv.db` 里存着你所有的数据库凭据和 AI API Key（加密的，但口令如果没改就等于明文）。**不要提交到版本库，备份和拷贝时按凭据文件对待。**

## 环境要求

- JDK 17 或以上
- MySQL、MariaDB、Oracle、SQL Server、DB2、PostgreSQL、openGauss、达梦、人大金仓、H2 的驱动均已内置；GBase、神通（Oscar）和自定义数据库需要在连接配置中填写厂商驱动 jar 路径（见 README「加载外部驱动 jar」一节）

## 完整文档

[README（中文）](https://github.com/vfaner/synctool/blob/main/README.md) · [README (English)](https://github.com/vfaner/synctool/blob/main/README_EN.md)
