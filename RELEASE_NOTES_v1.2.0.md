# SyncTool v1.2.0

异构数据库实时同步工具。开箱即用的可执行 jar，放到内网机器上 `java -jar` 就能跑，不需要源码、不需要 Maven。

这个版本整合了 v1.1.1、v1.1.2 以及此前未发布的改动，包含新功能和界面调整，因此版本号跳到 1.2.0。

## 本次更新

### 数据库连接区分「源」与「目标」

- 连接列表拆为**源数据库连接**、**目标数据库连接**两个栏目，各自独立分页
- 新建连接必须选择用途：源（数据读出）或目标（数据写入）
- 新建项目时源、目标下拉只列出对应用途的连接，不会再选错
- 升级后原有连接**首次启动自动归类**（按已有项目的引用关系推断，未被引用的默认为源），无需手工处理
- 已被项目占用的连接不能反向改用途，页面会提示相关项目名称

### 常用数据库驱动内置

MySQL、MariaDB、Oracle、SQL Server、DB2、PostgreSQL、openGauss、达梦、人大金仓、H2 共十种驱动随包内置，不再需要手动往服务器放 jar。GBase、神通（Oscar）和自定义类型仍需填写厂商驱动 jar 路径。

### 其他改进

- 新增「系统信息」页（原「设置」菜单改名）：显示当前版本、GitHub 最新版本与本版更新说明；更新检查在后台进行，**不拖慢页面，纯内网也能看当前版本和更新内容**，也可用 `--app.update-check.enabled=false` 关闭
- 变更日志页支持手动 / 自动刷新（5 秒一轮，不整页重载），第二页之后自动刷新自动停用以免阅读时行位跳动
- 新建连接时默认端口随数据库类型自动联动
- 修复删除正被项目占用的连接时错误提示显示为英文代码的问题

## 快速开始

```bash
java -jar synctool.jar
```

访问 <http://localhost:8080>，用下面的账号登录。

## 升级

换 jar、重启即可，配置和数据不动。本次会自动给元数据库加一列保存连接用途，启动日志出现 `Backfilled source/target role...` 即归类完成；升级后建议到连接页核对一眼，需要当目标用的连接在编辑页改用途即可。

**加密口令必须与之前一致**，否则已存的数据库密码无法解密。停机期间源库产生的变更，重启后由游标机制自动补齐。

从 v1.0.0 升级请先读 [v1.1.0 发布说明](https://github.com/vfaner/synctool/releases/tag/v1.1.0)（v1.1.0 起加入登录鉴权）。

## 首次启动必读

1. **改掉初始密码**：内置 `admin` / `view` 两个账号，初始密码都是 `123456`，登录后点右上角用户名修改；忘记密码无法找回，只能删除元数据库 `app_user` 表对应行后重启
2. **覆盖加密口令**：jar 内置公开默认密钥，首次保存连接前请通过 `--sync.crypto-password` 和 `--sync.crypto-salt` 设置自己的口令，否则存进去的数据库密码等于明文
3. **固定数据目录**：程序在启动目录下创建 `./data`（元数据库，含加密凭据）、`./logs`、`./snapshots`，请固定在同一目录启动；元数据库文件按凭据对待，不要提交到版本库

## 环境要求

- JDK 17 或以上
- GBase、神通（Oscar）和自定义数据库需要自备厂商驱动 jar

## 完整文档

[README（中文）](https://github.com/vfaner/synctool/blob/main/README.md) · [README (English)](https://github.com/vfaner/synctool/blob/main/README_EN.md)
